import styled from "styled-components";

const StyledMission = styled.section`
  
`

export default function Mission() : JSX.Element {
  return (
    <StyledMission>
      <h2>Never forget the legends</h2>
    </StyledMission>
  )
}