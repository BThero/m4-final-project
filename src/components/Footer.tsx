import styled from "styled-components";

const StyledFooter = styled.footer`
  
`

export default function Footer() : JSX.Element {
  return (
    <StyledFooter>
      <p>Help us preserve history</p>
    </StyledFooter>
  )
}